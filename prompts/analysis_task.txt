# Extraction Prompt (for reference)
{extraction_prompt}

# Extraction Result
sample_id: {sample_id}
status: {status}
raw_output: {raw_output}
parsed_output: {parsed_output}
{error_details}

# Sample Input
{sample_input}
{sample_metadata}

# Task
Based on the image content and the extraction result above, determine whether the extraction result is correct.
Do NOT rely on any ground truth or external reference - make your judgement solely from the image and extraction output.

Respond with a JSON object containing:
- "judgement": object with at least "is_correct" boolean
- "confirmed_facts": list of strings, facts you can confirm from the image
- "hypothesized_error_causes": list of strings, hypothesized error causes
- "prompt_section_attribution": list of objects, related prompt sections
- "error_reason": string or null, reason if the extraction is incorrect
