[project]
name = "mmap-optimizer"
version = "0.1.0"
description = "MMAP optimizer LLM artifact recording utilities"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
