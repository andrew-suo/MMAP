# Default Scenario

This is the default scenario for testing the MMAP optimizer.

## Files

- `scenario.yaml` - Scenario manifest
- `optimizer.yaml` - Optimizer configuration
- `data/` - Sample data
- `prompts/` - Prompt templates
- `schemas/` - Output schemas
