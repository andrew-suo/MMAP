name: Default Scenario
description: A default scenario for testing MMAP optimizer
optimizer_config: optimizer.yaml
data_dir: data
prompts_dir: prompts
schemas_dir: schemas
# section_id_hints: {}  # Optional: map heading keywords → section IDs for domain-specific matching
